// Merge two sorted arrays
#include <stdio.h>
#include "arrayUtils.h"

int main() {
    double arr1[] = {1, 3, 5, 7,  9};
    double arr2[] = {2, 4, 6, 8, 10};
    int arr1Size = ArrLen(arr1);
    int arr2Size = ArrLen(arr2);
    int mergeSize = arr1Size + arr2Size;

    double mergeSorted[mergeSize];


    return 0;
}
